# IPv4 Assigning

You can assign an IPv4 address either statically or dynamically

## Static

When you statically or manually assign an IPv4 address to a host or end device, we manually input 4 key pieces of information:

- Ipv4 address
- Subnet Mask 
- Default gateway address
- DNS server address

## Dynamic

- More to follow