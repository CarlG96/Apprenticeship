info:
  name: adminAmendStaffAnnualLeaveAllocation
  type: http
  seq: 20

http:
  method: PATCH
  url: http://localhost:8901/api/admin/staff/3/leave-allocation
  body:
    type: json
    data: |-
      {
        "leaveTypeName": "Annual Leave",
        "remaining": 75
      }
  auth:
    type: bearer
    token: "{{loginAdmin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
