info:
  name: adminApproveStaffLeave
  type: http
  seq: 21

http:
  method: PATCH
  url: http://localhost:8901/api/admin/leave-requests/25/approve
  auth:
    type: bearer
    token: "{{loginAdmin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
