info:
  name: adminGetAnalytics
  type: http
  seq: 22

http:
  method: GET
  url: http://localhost:8901/api/admin/analytics/leave-usage
  auth:
    type: bearer
    token: "{{loginAdmin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
