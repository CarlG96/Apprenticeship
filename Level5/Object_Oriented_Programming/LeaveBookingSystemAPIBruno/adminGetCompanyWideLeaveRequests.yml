info:
  name: adminGetCompanyWideLeaveRequests
  type: http
  seq: 17

http:
  method: GET
  url: http://localhost:8901/api/admin/leave-requests/outstanding
  auth:
    type: bearer
    token: "{{loginAdmin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
