info:
  name: adminGetManagersTeamsLeaveRequests
  type: http
  seq: 18

http:
  method: GET
  url: http://localhost:8901/api/admin/leave-requests/outstanding?managerId=5
  params:
    - name: managerId
      value: "5"
      type: query
  auth:
    type: bearer
    token: "{{loginAdmin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
