info:
  name: adminGetStaffsLeaveRequests
  type: http
  seq: 19

http:
  method: GET
  url: http://localhost:8901/api/admin/leave-requests/outstanding?staffId=3
  params:
    - name: staffId
      value: "3"
      type: query
  auth:
    type: bearer
    token: "{{loginAdmin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
