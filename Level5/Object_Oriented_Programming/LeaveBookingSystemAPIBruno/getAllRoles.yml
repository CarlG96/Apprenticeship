info:
  name: getAllRoles
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8901/api/role
  auth:
    type: bearer
    token: "{{loginAdmin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
