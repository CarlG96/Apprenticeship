info:
  name: getRoleById
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8901/api/role/1
  auth:
    type: bearer
    token: "{{loginAdmin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
