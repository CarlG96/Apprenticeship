info:
  name: loginAdmin
  type: http
  seq: 6

http:
  method: POST
  url: http://localhost:8901/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "admin@example.com",
        "password": "MyInitialAdminPassword"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
