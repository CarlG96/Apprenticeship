info:
  name: loginManager
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:8901/api/auth/login
  body:
    type: json
    data: |-
      {
        "email": "alice.manager@example2.com",
        "password": "ManagerPass123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
