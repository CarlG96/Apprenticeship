info:
  name: managerGetOutstandingLeaveRequestsForStaff
  type: http
  seq: 12

http:
  method: GET
  url: http://localhost:8901/api/manager/leave-requests/outstanding
  auth:
    type: bearer
    token: "{{loginManager}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
