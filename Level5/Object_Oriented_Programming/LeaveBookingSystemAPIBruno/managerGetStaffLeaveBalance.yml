info:
  name: managerGetStaffLeaveBalance
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:8901/api/manager/staff/6/leave-balance
  auth:
    type: bearer
    token: "{{loginManager}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
