info:
  name: staffViewLeaveBalance
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8901/api/staff/me/leave-balance
  auth:
    type: bearer
    token: "{{loginStaff}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
