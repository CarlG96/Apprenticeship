info:
  name: staffViewLeaveRequests
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:8901/api/staff/me/leave-requests
  auth:
    type: bearer
    token: "{{loginStaff}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
